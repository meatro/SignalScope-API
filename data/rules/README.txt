SignalScope loads /rules/active.ssrules first, then /rules/default.ssrules.

The web interface can save a tested package as active.ssrules. Keep reusable
packages in this folder and select them with POST /api/rules/select.
